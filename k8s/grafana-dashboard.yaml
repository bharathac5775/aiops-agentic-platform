apiVersion: v1
kind: ConfigMap
metadata:
  name: aiops-dashboard
  namespace: monitoring
  labels:
    grafana_dashboard: "1"
data:
  stress-app-dashboard.json: |
    {
      "title": "stress application cpu utilisation"
    }