apiVersion: v1
kind: Service
metadata:
  name: aiops-dashboard
  namespace: default
spec:
  selector:
    app: aiops-dashboard
  ports:
  - name: http
    port: 8501
    targetPort: 8501
  type: NodePort
